services:
  caddy:
    image: caddy:latest
    restart: always
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./Caddyfile:/etc/caddy/Caddyfile
      - caddy_data:/data
      - caddy_config:/config
    depends_on:
      - app

  moderation:
    image: ghcr.io/bigknoxy/jeetsocial2-moderation:latest
    build: ./moderation
    restart: always
    healthcheck:
      test: [ "CMD", "curl", "-f", "http://localhost:3001/moderate" ]
      interval: 30s
      timeout: 10s
      retries: 3

  app:
    image: ghcr.io/bigknoxy/jeetsocial2-app:latest
    build:
      context: .
      dockerfile: ./app/Dockerfile
    ports:
      - "3000:3000"
    depends_on:
      - moderation
    environment:
      - MODERATION_SERVICE_URL=http://moderation:3001
      - DATABASE_PATH=/app/data/social.db
      - NODE_ENV=production
    volumes:
      - sqlite_data:/app/data
    restart: always
    healthcheck:
      test: [ "CMD", "curl", "-f", "http://localhost:3000/posts" ]
      interval: 30s
      timeout: 10s
      retries: 3

volumes:
  sqlite_data:
  caddy_data:
  caddy_config:
